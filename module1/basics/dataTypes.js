"use strict"  // treats all code in newer verion of js, ab jaroorat nhi hoti waise but knowledge ke liye
//alert(3+3) we are using nodejs, not browser
let age = 18
let name = "aniket"
let ismale = true
let jobtype
let ienvioej = null

console.log(typeof(ienvioej));  // object. hence null is an object type. it is a standalone value.
console.log(typeof(jobtype))  // ubdefined. this means the variable is declared but not defined.
